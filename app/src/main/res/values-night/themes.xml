<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base dark application theme. -->
    <style name="Base.Theme.ToruShoppingApp" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your dark theme here. -->
        <item name="android:statusBarColor">@color/menu_dark</item> <!-- Dark status bar -->
        <item name="android:navigationBarColor">@color/menu_dark</item> <!-- Dark bottom navigation bar -->
    </style>

    <!-- Main theme that applies globally in night mode -->
    <style name="Theme.ToruShoppingApp" parent="Base.Theme.ToruShoppingApp">

    </style>

    <style name="CircleImageView">
        <item name="cornerFamily">rounded</item>
        <item name="cornerSize">15dp</item>
    </style>
</resources>
